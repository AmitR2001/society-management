# Society Management Backend

Production-ready MERN backend with JWT auth, RBAC, billing, Razorpay integration, and reporting.

## Run

1. `cd backend`
2. `npm install`
3. Copy `.env.example` to `.env` and update values.
4. `npm run seed:roles`
5. `npm run dev`
